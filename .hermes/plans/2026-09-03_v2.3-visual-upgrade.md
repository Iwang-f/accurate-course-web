# Accurate Course Web v2.3 — Visual Upgrade & Motion Graphics Plan

> **Skill chain:** plan → subagent-driven-development → verification-before-completion.
> **Working dir:** `/home/ubuntu/projects/accurate-course-prototype`

**Goal:** Website jasa kursus Accurate lebih eye-catching tanpa jadi slop. Semua halaman punya frame motion, tipografi dirapikan (justify + hierarchy), form/tabel konsisten shadcn, dan hero slot terisi motion graphics video simulasi software Accurate (self-made, bukan menunggu klien).

**Non-goals:** Tidak ganti palette. Tidak nambah dependency baru di luar yang sudah ada (`motion`, shadcn primitives). Tidak ubah struktur halaman.

---

## Phase 1 — Motion Graphics Video Simulasi Accurate (hero slot)

### Task 1.1: Build animated Accurate UI mockup component
**Objective:** Pure CSS/motion animated fake-UI dashboard — journal entry typing, numbers count up, status turns "BALANCE", bar chart grows.
**Files:**
- Create: `src/components/accurate-motion-mock.tsx`

**Steps:**
1. Build component: window chrome (traffic-light dots, title bar "Accurate"), sidebar nav skeleton, main area = jurnal table rows
2. Animate: rows appear stagger, angka count-up, "Laporan Berjalan" status chip turns green "BALANCE", mini bar chart bars grow
3. Loop 8–10 detik, `pointer-events-none`, respect reduced-motion (static final state)
4. Data: hardcoded in component, no site.ts seam (pure visual)

**Verification:** component renders in dev, reduced-motion shows static state, no layout overflow.

**Commit:** `feat: animated Accurate UI mockup component`

### Task 1.2: Wire mockup into HeroMedia video slot
**Objective:** Placeholder DRAFT video slot diganti animated mockup sampai klien kirim recording asli.
**Files:**
- Modify: `src/components/hero-media.tsx`

**Steps:**
1. Import `AccurateMotionMock` — render inside video frame when `site.heroVideo` empty
2. Keep video priority: `site.heroVideo` set → real video renders
3. Caption kecil "Simulasi tampilan" biar jujur, bukan fake screenshot

**Verification:** home page hero shows animated mockup, real-video path still intact.

**Commit:** `feat: wire animated mockup into hero slot`

### Task 1.3: Render mockup to MP4 (motion graphics video asset)
**Objective:** Export component sebagai video file untuk dipakai klien (social/IG) — bukan hanya live web.
**Files:**
- Create: `scripts/temp/render-mock-video.py` (Playwright screencast → ffmpeg → `public/media/accurate-walkthrough.mp4`)

**Steps:**
1. Playwright: load dev page, viewport 1280×720, record 10s screen video of mockup
2. ffmpeg: convert webm → mp4 H.264 (plays inline everywhere)
3. Save to `public/media/`, set `SITE.heroVideo = "/media/accurate-walkthrough.mp4"` as fallback
4. Cleanup temp script

**Verification:** `public/media/accurate-walkthrough.mp4` exists, plays in browser, < 2MB ideal.

**Commit:** `feat: render motion graphics video asset from mockup`

---

## Phase 2 — Frame Motion Polish

### Task 2.1: Frame motion di hero
**Objective:** Hero text + media punya entrance motion — headline reveal per-line, media scale-in.
**Files:**
- Modify: `src/app/page.tsx` (hero section)

**Steps:**
1. Split headline jadi 2 bagian, animate stagger reveal (opacity + y)
2. Media panel: scale 0.95→1 + fade saat mount
3. Chips: pop-in delay bertingkat
4. All respect reduced-motion

**Verification:** dev render, entrance smooth tanpa layout shift (CLS).

**Commit:** `feat: hero entrance frame motion`

### Task 2.2: Section transition frames
**Objective:** Section antar-block punya motion halus biar scroll terasa premium.
**Files:**
- Modify: `src/components/motion.tsx` (tambah `SectionFrame` wrapper jika perlu)
- Modify: `src/app/page.tsx` section wrappers

**Steps:**
1. Cek pattern: Reveal sudah ada — upgrade ke variant "frame" (subtle border draw / bg shift saat in-view)
2. Keep it restrained: max 1 frame effect per section, no rainbow

**Verification:** scroll home page — motion terasa tapi tidak norak.

**Commit:** `feat: section frame transitions`

---

## Phase 3 — Tipografi: Justify & Hierarchy

### Task 3.1: Justify body text panjang
**Objective:** Paragraf body panjang pakai `text-justify` untuk look editorial rapi.
**Files:**
- Modify: `src/app/page.tsx`, `src/app/tentang/page.tsx` (paragraf pendek panjang ≥ 2 kalimat)

**Steps:**
1. Tambah class `text-justify` (atau `text-pretty`/`text-balance` untuk heading) pada paragraf deskriptif
2. Hanya untuk teks ≥ 2 kalimat — label pendek tetap default
3. Mobile: `text-justify` di skip (hyphens jelek di layar sempit) — responsive: `sm:text-justify`

**Verification:** halaman tentang + home — paragraf rata kanan-kiri di desktop.

**Commit:** `feat: justify long-form body text`

### Task 3.2: Text balance untuk heading
**Objective:** Heading multi-line pakai `text-balance` biar wrap natural.
**Files:**
- Modify: `src/components/page-intro.tsx`, hero headline

**Steps:**
1. `text-balance` di h1/h2 yang bisa wrap 2+ baris
2. `text-pretty` di paragraf pendek biar orphans mati

**Verification:** heading tidak ada widow/orphan jelek di 360px & desktop.

**Commit:** `feat: text-balance on headings`

---

## Phase 4 — shadcn Consistency Sweep

### Task 4.1: Audit sisa raw HTML
**Objective:** Pastikan semua interactive/form/table elements pakai shadcn.
**Files:**
- Read: semua `src/app/*.tsx`, `src/components/*.tsx`

**Steps:**
1. grep raw `<button`, `<input`, `<select`, `<textarea`, `<table` di luar `src/components/ui/`
2. Replace yang ketemu (submit button kontak → shadcn Button sudah; cek footer link list, FAQ accordion)
3. FAQ: cek apakah sudah shadcn Accordion — jika belum, convert

**Verification:** grep clean — 0 raw interactive elements di luar ui/.

**Commit:** `chore: shadcn consistency sweep`

---

## Phase 5 — Verify & Deploy

### Task 5.1: Full check + mobile verify + push
**Steps:**
1. `npm run check` (typecheck + lint + 34 tests + build)
2. Playwright 360px sweep semua page — scrollWidth == 360, no overflow
3. Reduced-motion check: static fallback render
4. Commit + push origin main → Vercel
5. Verify production URL + new hero video plays

**Commit:** `chore: v2.3 final check and deploy`

---

## Verification Checklist

- [ ] Hero menampilkan animated Accurate mockup (bukan placeholder DRAFT)
- [ ] `public/media/accurate-walkthrough.mp4` ada, < 2MB, plays inline
- [ ] Hero entrance motion smooth, no CLS
- [ ] Paragraf panjang justified di desktop
- [ ] Heading text-balance
- [ ] grep raw interactive elements: 0
- [ ] `npm run check` pass (0 errors)
- [ ] 360px sweep: no horizontal scroll di 5 halaman
- [ ] Reduced-motion: static fallback
- [ ] Vercel deploy Ready + verified live

## Blockers Note

Placeholder data klien (WA, testimoni, angka statistik) tetap — jangan claim video mockup = product asli. Caption "Simulasi tampilan" wajib.
